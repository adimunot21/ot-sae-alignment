# Open Questions

Tracked questions from the Phase 1 plan. Each one gets resolved or explicitly
deferred by end of Phase 1.

- **Float precision.** 64-bit by default in Phase 1. Revisit when arrays get large.
- **Cost matrix abstraction.** Pre-computed `C` only for now. Lazy costs deferred.
- **Sinkhorn convergence criterion.** Lean: marginal-violation L1, log it in return value.
- **Multiplicative Sinkhorn exposure.** Lean: keep in `_legacy`, document as reference only.
- **Derivation format.** Markdown with KaTeX math.
